# 更新日志

本文件记录技能的版本变更。版本号遵循语义化版本规范。

## [1.0.1] - 2026-09-20

针对首次实战发现的两类问题作修正：交付层面的格式缺陷，以及分析框架缺少行业层级。

### 新增：行业环境层

- 核心方法补入前置环节「先建立行业参照」。战略分析的标准顺序是行业、公司、判断；跳过行业直接分析公司，会导致三处落空：无法说明公司利润率的行业成因、无法判断外部压力属行业性还是个体性、无法判断转型的时间窗口是否存在。
- 研报模板新增「行业环境」章节，列为必选，置于公司概况之前，含市场容量与生命周期、行业盈利水平与竞争结构、成本与技术特征、规模经济与范围经济、纵向整合与客户结构五节。
- 研报模板新增「分析覆盖范围」表，逐项声明行业经济特征各维度的覆盖状态。取不到数据的维度须如实声明，不得沉默跳过。
- 数据采集清单新增「行业特征」一节，含十三项维度及其来源与层级，并注明同行业公司招股说明书是性价比最高的行业数据来源。

### 新增：交付前的格式自检

- **HTML 正文不得出现 Markdown 语法。** 加粗须写 `<strong>`，列表写 `<ul>`／`<ol>`，表格写 `<table>`。成稿后须检索 `**` 与行首 `#`，两者均须为零。
- **柱状图的数据编码规则。** 正负须以方向区分，设标注为 0 的零线，正值向上、负值向下；仅用颜色区分正负属错误编码。柱长须与数值成比例，同一张图内各柱的「像素高度÷数值」比值须一致，并给出比例尺的计算方法。
- **SVG 文字坐标自检。** 所有文字坐标须加上所属分组的偏移量后仍在画布内；负值柱的数值标签应置于柱底下方。附五项自检清单。
- **窄屏响应式规则。** 缺少该规则时，多列的数据来源表在手机上会溢出。
- **打印页码的处理说明。** CSS 页码计数器只在支持分页媒体的渲染引擎中生效，浏览器直接打印时不会产生页码，不得仅依靠样式表而在报告中宣称已含页码。

### 修改：待核实数据的使用限度

- 明确待核实数据不得成为核心结论的唯一支撑。判断办法是把该项整体删除后，主要判断是否仍然成立；若不成立，须补做核实、改为条件式表述，或在正文中以弱化语气交代来源。
- 风险提示中不得笼统声称「该缺失不影响结论方向」，须逐项说明哪个结论依赖待核实数据、依赖程度、以及核对结果不符时结论如何变化。
- 说明「未核实」与「待核实」是两个不同状态，页脚声明不得使读者误以为正文数据全部已核实。

### 修改：其他

- 全部参考文件的章节标题去掉序数词前缀，改为直接写名称。
- 示例报告同步修正：清除正文残留的 Markdown 加粗语法、重绘图 3 使柱长与数值成比例、补入窄屏响应式与打印页边距规则、补入分析覆盖范围表、修正数据边界表述与来源层级标注。

## [1.0.0] - 2026-09-19

首个公开发布版本。

### 核心方法

- 三步追问法：收入来源 → 风险敞口 → 可转换性。追问顺序不可调换，每一步的结论构成下一步的前提。
- 风险敞口扩展为三个端口：支付端、采购端、结算端。其中结算端（汇率）为出口导向型企业必查项。
- 可转换性判断设三种落点：换不动（结构性风险）、换得动但慢（过渡期风险）、换得动（阶段性困难）。
- 抗风险能力的两层分辨：来源（主动设计或周期巧合）与可控性（能否自主掌握）。

### 输出形态

- 交付物为单文件 HTML 研报，样式内联，不依赖外部资源，支持离线显示与打印。
- 正文含不少于四张图表，分别承载收入构成、时间趋势、同群对照、体量缺口四类判断。
- 每张图表强制三行标注：标题、数据来源、读图要点。
- 涨跌配色遵循 A 股惯例，涨红跌绿。
- 可选的投资建议章节，按投资者立场分档给出判断依据，不给出具体操作。

### 数据来源规范

- 建立三级来源分级：一级·原始披露、二级·可核来源、三级·线索性来源。
- 判级标准为两个问题：发布主体是谁，能否取得可下载的原件。
- 三级来源仅可用于发现线索，不得作为论据。
- 明令禁止八类来源，包括无署名网络文章、算法生成的信息聚合页、荐股群"内部消息"等。
- 硬规则：正文不得出现无来源的数字；媒体的分析性判断不得引用。
- 数据来源表登记七个字段：数据项、数值/口径、层级、来源名称、日期、定位、状态。

### 写作规范

- 采用学术语体，明确术语替换口径与判断句的规范范式。
- 出现频率较高的五种分析陷阱写入 `analysis-guide.md`。
- 与去 AI 味规则集（`lieflat-less-ai-tone`）的兼容关系已说明：前者针对修辞习惯，后者针对术语与句式规范，两者可同时满足。
